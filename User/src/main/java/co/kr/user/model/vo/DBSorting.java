package co.kr.user.model.vo;

/**
 * 데이터베이스 정렬 순서(Direction)를 정의하는 Enum 클래스입니다.
 */
public enum DBSorting {
    /** 기본 정렬 순서 */
    DEFAULT,
    /** 오름차순 (Ascending) */
    ASC,
    /** 내림차순 (Descending) */
    DESC
}