name: 유저 서비스 CI 스크립트

on:
  pull_request:
    branches:
      - main
      - dev
    paths:
      - "User/**"

jobs:
  execute-test:
    uses: ./.github/workflows/ci-workflow.yml
    secrets: inherit
    with:
      service-name: User
