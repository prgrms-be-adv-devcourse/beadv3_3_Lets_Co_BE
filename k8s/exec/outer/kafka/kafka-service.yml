apiVersion: v1

kind: Service

metadata:
  name: kafka-service

spec:
  clusterIP: None
  selector:
    app: kafka

  ports:
    - name: broker
      port: 9090
      targetPort: 9090
    - name: controller
      port: 9091
      targetPort: 9091

